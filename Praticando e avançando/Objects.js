// Object

const person = {
    name: 'John',
    age: 30,
    weight: 88.6,
    isAdmin: true
}

// para invocar alguma propriedade dentro do objeto
console.log(`${person.name} tem ${person.age} anos`)